tiley_sources = files(
    'tiley.cpp'
)

subdir('events')
subdir('lib')
subdir('wrap')
subdir('utils')
subdir('renderer')

all_tiley_sources = tiley_sources + events_sources + wrap_sources + lib_sources + utils_sources + renderer_sources