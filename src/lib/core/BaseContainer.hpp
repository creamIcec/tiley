#pragma once

namespace tiley{
    
    // A container represents a parent for splitting screen or a window.
    // Note: Different layout algorithm can use different data structures, not only binary tree.
    class BaseContainer{
        public:
            
    };
}